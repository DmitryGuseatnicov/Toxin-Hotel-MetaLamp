mixin widget-title(options = {})
  -
    const { level = "3", text = "" } = options

  if (level === "1")
    h1.widget-title.widget-title_big= text

  if (level === "2")
    h2.widget-title.widget-title_medium= text

  if (level >= "3")
    h3.widget-title.widget-title_small= text
